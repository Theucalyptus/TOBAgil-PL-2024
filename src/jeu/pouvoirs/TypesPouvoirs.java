package jeu.pouvoirs;

/**Enumération de tous les pouvoirs du Jeu. */
public enum TypesPouvoirs {
    /**Les alchimistes. */
    ALCHIMISTES,
    /**Les Ancestraux. */
    ANCESTRAUX,
    /**Les Armes. */
    ARMES,
    /**Les deux héros. */
    AUX_DEUX_HEROS,
    /**Les Batisseurs. */
    BATISSEURS,
    /**Les Berserks. */
    BERSERKS,
    /**Les cavernes. */
    DES_CAVERNES,
    /**Les collines. */
    DES_COLLINES,
    /**Les Forêts. */
    DES_FORETS,
    /**Les marais. */
    DES_MARAIS,
    /**Les diplomates. */
    DIPLOMATES,
    /**Les durs à cuire. */
    DURS_A_CUIRE,
    /**Et leur dragon. */
    ET_LEUR_DRAGON,
    /**Les Fortunes. */
    FORTUNES,
    /**Les Marchands. */
    MARCHANDS,
    /**Les Marins. */
    MARINS,
    /**Les Montés. */
    MONTES,
    /**Les Pillards. */
    PILLARDS,
    /**Les Scouts. */
    SCOUTS,
    /**Les Volants. */
    VOLANTS,
    /**La Tribu oubliée. */
    TRIBU_OUBLIEE
}
