package jeu;

/**Décrit les différent états du jeu. */
public enum JeuState {
    /**Le jeu est en attente de démarrage. */
    PAS_COMMENCEE,
    /**La partie est en cours. */
    EN_COURS,
    /**La partie est terminée. */
    TERMINEE
}
