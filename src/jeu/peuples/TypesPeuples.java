package jeu.peuples;

/**Enumération des différents types de peuple. */
public enum TypesPeuples {
    /**Les Amazones. */
    AMAZONES,
    /**Les Elfes. */
    ELFES,
    /**Les Geants. */
    GEANTS,
    /**Les Hommes-Rats. */
    HOMME_RATS,
    /** Les Humains. */
    HUMAINS,
    /**Les Mages. */
    MAGES,
    /**Les Mi-Portions. */
    MIPORTIONS,
    /**Les Nains. */
    NAINS,
    /**Les Orcs. */
    ORCS,
    /**Les sorciers. */
    SORCIERS,
    /**Les Squelettes. */
    SQUELETTES,
    /**Les Tritons. */
    TRITONS,
    /**Les Trolls. */
    TROLLS,
    /**Les Zombies. */
    ZOMBIES,
    /**Les Tribus Oubliées. */
    TRIBU_OUBLIEE
}
