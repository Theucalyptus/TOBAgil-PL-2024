package jeu.batiments;

/**Enumeration des types de Batiments. */
public enum TypesBatiments {
    /**Modélise l'absence de baiments. */
    AUCUN,
    /**Modelise un campement. */
    CAMPEMENT,
    /**Modelise une forteresse. */
    FORTERESSE,
    /**Modelise une antre de Troll. */
    ANTRE_DE_TROLL,
    /**Modelise une taniere. */
    TANIERE
}
