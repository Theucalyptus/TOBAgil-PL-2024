package jeu;

/**Enumération des différents types de d'effets supplémentaires d'une case. */
public enum TypesSymboles {
    /** Modélise une cavernes. */
    CAVERNES,
    /**Modélise une mine. */
    MINE,
    /**Modélise une source magique. */
    SOURCE_MAGIQUE,
    /**Modélise l'absence d'élement spécial. */
    AUCUN
}
