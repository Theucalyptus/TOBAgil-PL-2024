package jeu;

/** L'énumération de l'ensemble des régions possibles du jeu. */
public enum TypesRegions {
    /**Modélise un champ. */
    CHAMP,
    /**Modélise une forêt. */
    FORET,
    /**Modelise une colline. */
    COLLINE,
    /**Modélise un marais. */
    MARAIS,
    /**Modélise une montagne. */
    MONTAGNE,
    /**Modélise un lac ou une mer. */
    MER_ET_LAC
}
